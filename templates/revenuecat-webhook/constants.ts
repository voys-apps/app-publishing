/** Edit per app — store reverse-DNS ids must match Play / ASC / RC. */
export const CREDIT_PRODUCTS: Record<string, number> = {
  'com.example.app.credits10k': 10_000,
  'com.example.app.credits25k': 25_000
}

export const PRO_MONTHLY_CREDITS = 5_000

export const PRO_ENTITLEMENT_MARKERS = ['pro']
