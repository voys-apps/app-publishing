# AdMob — API note (do not use as default)

AdMob inventory APIs need **user OAuth** (not Play SA). `apps.create` is often
**403**. Voys default: **Console only** — open pages, collect keys.

Do not store Desktop OAuth client secrets in the app repo for AdMob.
